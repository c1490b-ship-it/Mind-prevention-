# Contributing

- All changes must be via Pull Request.
- Require at least 1 review from a CODEOWNER for sensitive directories.
- Do not include credentials, tokens, private keys, or PII in commits.
- Run `pre-commit` locally before pushing.
